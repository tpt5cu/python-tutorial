- The collections module contains many, many classes. These classes are less flexible, but are good for efficiently drawing large collections of
  objects that have the same properties (e.g. line segments)
- See the following for more fine-grained plotting functions:
  -  matplotlib_/axes_/_axes_/Axes_/methods.py