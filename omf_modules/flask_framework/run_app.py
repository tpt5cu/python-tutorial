"""
To run a flask application, find the file that has the Flask(__name__).
Set FLASK_APP to be that file, e.g. $ export FLASK_APP=./omf/web.py
Set debugging to be on with $ export FLASK_DEBUG=1
Do $ flask run

This is cool, but it doesn't set the other options that web.py main method does. 
"""