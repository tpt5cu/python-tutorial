# http://flask.pocoo.org/docs/1.0/api/

"""redirect(): redirects the client to the target url specified.

url_for(): generates a url using the passed endpoint (i.e. routing function) and passes any key-value
arguments to the endpoint. The key of each argument must match the parameter name in the function endpoint.
"""
